classes:
- service.telegraf.agent
- system.telegraf.agent.input.http_listener
- system.telegraf.sudo
parameters:
  telegraf:
    agent:
      output:
        prometheus_client:
          bind:
            address: 0.0.0.0
            port: 9126
          engine: prometheus
