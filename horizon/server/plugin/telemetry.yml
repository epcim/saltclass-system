parameters:
  horizon:
    server:
      plugin:
        telemetry:
          metric:
            engine: graphite
            host: ${_param:openstack_telemetry_address}
            port: 80
          source:
            engine: pkg
            name: openstack-dashboard-telemetry