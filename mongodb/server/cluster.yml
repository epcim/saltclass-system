classes:
- service.keepalived.cluster.single
- service.mongodb.server.cluster
parameters:
  _param:
    mongodb_server_replica_set: mongodb
    mongodb_master: mdb01.${_param:cluster_domain}
    mongodb_server_members:
    - host: mdb01
      priority: 2
    - host: mdb02
    - host: mdb03
