classes:
  - system.jenkins.slave
parameters:
  linux:
    system:
      user:
        jenkins:
          groups:
            - docker
      sudo:
        users:
          jenkins:
            hosts:
            - ALL
            commands:
            - docker