parameters:
  _param:
    linux_repo_contrail_component: oc30
    contrail_repo_pin_priority: 200
  linux:
    system:
      repo:
        tcpcloud_opencontrail:
          source: "deb [arch=amd64] http://apt-mk.mirantis.com/nightly/ ${_param:linux_system_codename} extra ${_param:linux_repo_contrail_component}"
          architectures: amd64
          key_url: "http://apt-mk.mirantis.com/public.gpg"
          pin:
          - pin: 'release c=oc30'
            priority: ${_param:contrail_repo_pin_priority}
            package: '*'
