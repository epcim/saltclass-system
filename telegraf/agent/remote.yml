classes:
- service.telegraf.agent.remote
parameters:
  telegraf:
    remote_agent:
      input:
        internal:
      output:
        prometheus_client:
          bind:
            address: 0.0.0.0
            port: 9126
          engine: prometheus
