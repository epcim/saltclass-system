classes:
  - system.jenkins.slave
parameters:
  linux:
    system:
      user:
        jenkins:
          groups:
            - docker
      sudo:
        users:
          jenkins:
            setenv: true
            hosts:
            - ALL
            commands:
            - docker