parameters:
  horizon:
    server:
      plugin:
        billing:
          metric:
            engine: graphite
            host: ${_param:stacklight_telemetry_address}
            port: 80
          config:
            currency: CZK
            allocation: false
            extra_resource_types: false
          source:
            engine: pkg
            name: openstack-dashboard-billing